<% events.each do |event| %>
  <turbo-frame id="<%= dom_id event %>" class='w-full flex flex-col gap-2 border-2 px-10 py-5'>
    <%= render(
      partial: 'partials/events/card',
      locals: { event: }
    ) %>
    <%= render(
      partial: 'partials/buttons/goto_profile',
      locals: { event: }
    ) %>
  </turbo-frame>
<% end %>
