<div class='flex justify-start gap-2'>
  <% if user_signed_in? %>
    <%= link_to(
      "Go to event's page",
      event_path(event),
      class: 'btn-session',
      data: { turbo: false }
    ) %>
  <% end %>
</div>
